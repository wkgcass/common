package net.cassite.pure.aop;

public abstract class ExceptionWeaver extends Weaver {
        @Override
        protected final void before(AOPPoint point) {
        };

        @Override
        protected final void after(AOPPoint point) {
        };
}
